{
  "name": "aegis-protocol",
  "version": "1.0.0",
  "description": "Privacy Infrastructure on Base Chain",
  "author": "AEGIS Protocol",
  "license": "MIT",
  "keywords": ["ethereum", "base", "privacy", "zeroknowledge", "zksnark"],
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^5.7.2"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network baseGoerli",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base",
    "test": "hardhat test"
  }
}
